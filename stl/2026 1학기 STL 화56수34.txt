//------------------------------------------------------
// 2026 1학기 화56수34                 3월 3일          (1주 1일)
//------------------------------------------------------
// vs버전 - 17.14.27 (추가로 업데이트 해야 함)
// 컴파일 환경 - Release / x64
// 프로젝트 설정 - 일반 - /std::c++lastest
//			   - c/c++ - SDL 설정 - 아니요
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준

	

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII

	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260303);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 3일          (1주 1일)
//------------------------------------------------------
// vs버전 - 17.14.27 (추가로 업데이트 해야 함)
// 컴파일 환경 - Release / x64
// 프로젝트 설정 - 일반 - /std::c++lastest
//			   - c/c++ - SDL 설정 - 아니요
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준

	

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII

	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260303);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 3일          (1주 1일)
//------------------------------------------------------
// vs버전 - 17.14.27 (추가로 업데이트 해야 함)
// 컴파일 환경 - Release / x64
// 프로젝트 설정 - 일반 - /std::c++lastest
//			   - c/c++ - SDL 설정 - 아니요
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준

	

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII

	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260303);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	std::cout << now << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);

	std::cout << UTC << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);

	std::cout << UTC << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);

	std::cout << now << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	std::cout << now << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	std::cout << UTC << '\n';

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	using namespace std::chrono_literals;
	std::cout << now + 9h << '\n'; // c++ 14는 now가 오버로딩이 안됐다

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	using namespace std::chrono_literals;
	std::cout << now + 9h << '\n'; // c++ 14는 now가 오버로딩이 안됐다

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	using namespace std::chrono_literals; // using은 define을 대신하는 용도로 사용된다

	std::cout << now + 9h << '\n'; // c++ 14는 now가 오버로딩이 안됐다

	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}======================================================================
저장시간 - 2026-03-04 11:56:35.6632297
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 작성한다면 어떻게 할지 따로 생각해봐라
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자
	
	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	using namespace std::chrono_literals; // using은 define을 대신하는 용도로 사용된다

	
	out << "======================================================================" << '\n';
	out << "저장시간 - " << now + 9h << '\n'; // c++ 14는 now가 오버로딩이 안됐다
	out << "======================================================================" << '\n';
	out << '\n';
	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {}, 
		std::ostream_iterator<char>{out});

}======================================================================
저장시간 - 2026-03-04 12:05:40.8630453
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

======================================================================
저장시간 - 2026-03-04 12:06:31.8912781
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}


======================================================================
저장시간 - 2026-03-04 12:07:02.8465894
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}

======================================================================
저장시간 - 2026-03-04 12:09:34.5383065
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}======================================================================
저장시간 - 2026-03-04 12:09:36.2289143
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}======================================================================
저장시간 - 2026-03-04 12:09:57.2750964
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}
======================================================================
저장시간 - 2026-03-04 12:10:00.3715027
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save(const std::string& ); // 함수의 선언문: 이름, 인자의 타입과 개수, 반환되는 타입

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}
======================================================================
저장시간 - 2026-03-04 12:10:59.9053489
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

#include "save.h"

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	// c++ 표준 상에서는 retrun 0;이 없어도 0을 반환, 안넣는게 표준
	
	// c++에서 사용하지 말았으면 좋겠는 자료형 char* T* T[N]
	// 이 3개는 대체품이 명확히 존재해서 별루

	// int t[100]; T[N]의 대체품은 std::array<int, 100> t;
	std::array<int, 100> t; // zero-overhead 시간상 불리함이 없다. T[N] 쓸 필요가 없다

	t.size(); // 점을 찍어서 사용할 수 있는 멤버함수가 있다

	save("메인.cpp");						// "메인.cpp"를 저장
}
======================================================================
저장시간 - 2026-03-04 12:14:27.0639747
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>

#include "save.h"

//--------
int main()
//--------
{
	std::cout << "2026년 3월 4일 - save 완성" << '\n';

	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:14:29.1437810
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>

#include "save.h"

//--------
int main()
//--------
{
	std::cout << "2026년 3월 4일 - save 완성" << '\n';

	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:15:31.5948434
======================================================================

//-----------------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026/3/4																						송광민															
//-----------------------------------------------------------------------------------------------------------


#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>


void save(const std::string&);
======================================================================
저장시간 - 2026-03-04 12:15:31.6034177
======================================================================

//-----------------------------------------------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026/3/4																						송광민															
//-----------------------------------------------------------------------------------------------------------

#include "save.h"

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다." << '\n';

	std::ifstream in{ fname };				// RAII


	if (not in) {
		std::cout << '\"' << fname << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL 화56수34.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	//std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 작성한다면 어떻게 할지 따로 생각해봐라
	auto now = std::chrono::system_clock::now(); // deduction해서 auto로 쓰자

	auto UTC = std::chrono::system_clock::to_time_t(now);
	std::localtime(&UTC);
	using namespace std::chrono_literals; // using은 define을 대신하는 용도로 사용된다


	out << "======================================================================" << '\n';
	out << "저장시간 - " << now + 9h << '\n'; // c++ 14는 now가 오버로딩이 안됐다
	out << "======================================================================" << '\n';
	out << '\n';
	// in의 내용을 읽어서 out에 덧붙이자
	//std::copy(원본, 복사본);
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});

	out << '\n';

}
======================================================================
저장시간 - 2026-03-04 12:15:31.6101726
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// save 한 시간을 기록한다
// save를 분리한다
// -----------------------------------------------------
#include <iostream>

#include "save.h"

//--------
int main()
//--------
{
	std::cout << "2026년 3월 4일 - save 완성" << '\n';
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}
======================================================================
저장시간 - 2026년 03월 04일 수요일 12시 29분 59.4168607초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

//--------
int main()
//--------
{
	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:33:27.7449375
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

void change(int& x, int& y)
{
	int temp = x;
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	int a{ 1 }, b{ 2 };
	// [문제] change를 호출해서 다음과 같이 출력되게 하라

	change(a, b);

	cout << a << ", " << b << endl; // 2, 1

	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:38:26.4134630
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

void change(int* x, int* y) // change와 main은 다른 곳에 있으므로 주소를 보내야 한다
{
	int temp = *x;
	*x = *y;
	*y = temp;
}

//--------
int main()
//--------
{
	int a{ 1 }, b{ 2 };
	// [문제] change를 호출해서 다음과 같이 출력되게 하라

	change(&a, &b);

	cout << a << ", " << b << endl; // 2, 1

	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:47:50.0932957
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
private:
	int age;
public:
	Dog(const Dog& d){
				age = d.age;
	}
	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d){
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

};

void change(int& x, int& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	int temp{ x };
	x = y;
	y = temp;
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:53:05.2540793
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d){
				age = d.age;
	}
	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d){
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}


private:
	int age{};

};

void change(int& x, int& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	int temp{ x };
	x = y;
	y = temp;
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 12:57:54.3035279
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	/*friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}*/

	operator int() {
		return age;
	}


private:
	int age{};

};

void change(int& x, int& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	int temp{ x };
	x = y;
	y = temp;
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 13:02:25.9097482
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

void change(int& x, int& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	int temp{ x };
	x = y;
	y = temp;
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 13:03:51.3660397
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

void change(int& x, int& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	int temp{ x };
	x = y;
	y = temp;
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}

//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-04 13:15:26.6145450
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (1주 2일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

template <class T>

void change(T& x, T& y) // 레퍼런스 가면을 씌우면 코드가 깔끔해진다
{
	T temp{ x };
	x = y;
	y = temp;
}


//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-10 13:31:09.4710034
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

template <class T> // 템플릿 선언
void change(T& x, T& y) // 템플릿 정의 - 동시에 해야 함
{
	T temp{ x };
	x = y;
	y = temp;
}


//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026-03-10 13:32:00.5145296
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

template <class T> // 템플릿 선언
void change(T& x, T& y) // 템플릿 정의 - 동시에 해야 함
{
	T temp{ x };
	x = y;
	y = temp;
}


//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026년 03월 10일 화요일 13시 33분 02.0683807초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

template <class T> // 템플릿 선언
void change(T& x, T& y) // 템플릿 정의 - 동시에 해야 함
{
	T temp{ x };
	x = y;
	y = temp;
}


//--------
int main()
//--------
{
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 01분 32.7347937초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다


//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); 

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 16분 53.5660748초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다


//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	y = temp;
}
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 17분 48.6961732초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다


//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	//y = temp;
}
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 18분 07.8775963초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다
void change(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	//y = temp;
}

//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 19분 52.6217737초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <>
void change<Dog>(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	//y = temp;
}

//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 20분 33.6130841초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <> // 템플릿 특수화 
void change<Dog>(Dog& x, Dog& y)
{
	Dog temp{ x };
	x = y;
	//y = temp;
}

//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 25분 09.6380864초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <> // 템플릿 특수화(specialization)
void change<Dog>(Dog& x, Dog& y)
{
	cout << "개소리" << endl;
}

//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

//
// template <> // 템플릿 특수화(specialization)
//void change<Dog>(Dog& x, Dog& y)
//{
//	cout << "개소리" << endl;
//}
// 명시적으로 Dog의 change를 만나기 전에 써놓아야한다.
// 안그러면 이미 chage는 general하게 만들어져 있다
//
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 25분 20.4009191초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <> // 템플릿 특수화(specialization)
void change<Dog>(Dog& x, Dog& y)
{
	cout << "개소리" << endl;
}

//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

//
// template <> // 템플릿 특수화(specialization)
//void change<Dog>(Dog& x, Dog& y)
//{
//	cout << "개소리" << endl;
//}
// 명시적으로 Dog의 change를 만나기 전에 써놓아야한다.
// 안그러면 이미 chage는 general하게 만들어져 있다
//
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 26분 42.7080313초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <> // 템플릿 특수화(specialization)
void change<Dog>(Dog& x, Dog& y)
{
	cout << "개소리" << endl;
}

void change(Dog& x, Dog& y)
{
	cout << "global function" << endl;
}


//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

//
// template <> // 템플릿 특수화(specialization)
//void change<Dog>(Dog& x, Dog& y)
//{
//	cout << "개소리" << endl;
//}
// 명시적으로 Dog의 change를 만나기 전에 써놓아야한다.
// 안그러면 이미 chage는 general하게 만들어져 있다
//
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 27분 09.9269366초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>
#include <algorithm> // _로 시작하는 변수같은게 많은 이유 - 니가 쓸 게 아니니까 _로 시작하지 마라

#include "save.h"
using namespace std; // 강의를 위해 어쩔 수 없이 사용

class Dog{
public:
	Dog() = default;
	Dog(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 생성자가 완벽하게 작동하기 때문이다.
				age = d.age;
	}

	Dog(int a) : age{ a } {}
	
	Dog& operator=(const Dog& d) { // 만들 필요가 없었다. 컴파일러가 만들어주는 복사 대입 연산자가 완벽하게 작동하기 때문이다.
				age = d.age;
				return *this;
	}

	friend std::ostream& operator << (std::ostream& out, const Dog& d)
	{
		out << d.age;

		return out;
	}

	


private:
	int age{};

};

// compiler phase라는 게 있는데 그 때 템플릿이 인스턴스화 된다. 
// 템플릿의 단점은 코드가 확장될 때 오류 메세지가 어렵다

template <class T> 
void change(T& x, T& y) 
{
	T temp{ x };
	x = y;
	y = temp;
}
// 자료형이 무관한 generic한 코드만 템플릿으로 쓸 가치가 있다

template <> // 템플릿 특수화(specialization)
void change<Dog>(Dog& x, Dog& y)
{
	cout << "개소리" << endl;
}

void change(Dog& x, Dog& y) // 우선순위가 템플릿보다 높다
{
	cout << "global function" << endl;
}


//--------
int main()
//--------
{

	// 이름이 있는 것들은 전부 프로그램 실행 단계에 메모리 주소가 정해진다
	// free store에 만들어지는 메모리는 프로그램 실행 단계에 주소가 정해지지 않는다. 런타임에 주소가 정해진다. 그래서 이름을 붙일 수 없다
	{
		int a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b); //change<int>(a, b)가 기본인데 함수 호출 시에서는 제외할 수 있다. 클래스는 얄짤없다

		cout << a << ", " << b << endl; // 2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		// [문제] change를 호출해서 다음과 같이 출력되게 하라

		change(a, b);

		cout << a << ", " << b << endl; // 2, 1
	}


	save("메인.cpp");
}

//
// template <> // 템플릿 특수화(specialization)
//void change<Dog>(Dog& x, Dog& y)
//{
//	cout << "개소리" << endl;
//}
// 명시적으로 Dog의 change를 만나기 전에 써놓아야한다.
// 안그러면 이미 chage는 general하게 만들어져 있다
//
======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 44분 51.5157815초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <typename T>
T add(T a, T b)
{
	return a + b;
}

//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 0310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 46분 08.8548853초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <typename T>
T add(T a, T b)
{
	return a + b;
}

template <>
int add<int>(int a, int b)
{
	cout << a << " " << b << endl;
	return a + b;
}

//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 0310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 48분 55.7951828초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <typename T>
T add(T a, T b)
{
	return a + b;
}


//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 49분 37.8098826초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}


//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 14시 51분 42.4660123초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// template
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;
}


//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 00분 39.9198326초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// C++ 언어에서 template의 역할은? -> Generic Programming의 핵심 keyword - 모든 것이 템플릿으로 이루어진 세상이다
// 
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>

using namespace std;

#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;	// RVO 최적화
}

//
// T add(T a, T b)
//{
//	return a + b;
//}
// 
// const하고 & 뺀거가 함정문제일거임
//

// 템플릿 단점 - 소스코드 감출 수 없다  오류 메세지가 어려워진다, 느려지는게 아니라 오히려 컴파일러가 최적화해서 빨라질 수도 있다

// c++ 은 멀티 패러다임 언어(어떤 언어인가요? 나도 몰라) - 절차적, 객체지향, 함수형, 제네릭 프로그래밍 지원 - 템플릿은 c++이 원조고 제왕이다
// 함수형은 더 좋은 언어가 잇음
// c++의 자랑거리는 일반화고 객체지향 잘한다 하면 자바족이 싸움건다


//--------
int main()
//--------
{
	// [문제] template 함수 add를 만들어 add로 실행되게 하자

	cout << add(2026, 310) << endl;
	cout << add( "2026년"s, " 3월 10일"s ) << endl;


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 04분 51.1640655초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 10.4232885초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		if (arr[i] < 10000 && -10000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 19.3012037초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		if (arr[i] < 100000 && -100000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 26.5083951초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 31.3176205초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 33.8009292초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 06분 52.1826370초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"



//--------
int main()
//--------
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하고, 모든 값을 화면에 출력하라
	std::random_device rd;

	// random_device 를 통해 난수 생성 엔진을 초기화 한다.
	std::mt19937 gen(rd());

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = gen();
		//if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 09분 35.1915085초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = dre();
		//if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 11분 33.9184565초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 1000000 && -1000000 < arr[i])
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 12분 06.7394245초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		if (arr[i] < 0 )
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 13분 14.1630991초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ numeric_limits<int>::min(), 2147483647}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		if (arr[i] < 0 )
			cout << arr[i] << " ";
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 13분 34.3113156초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ numeric_limits<int>::min(), 2147483647}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
			cout << arr[i] << '\n';
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 13분 58.2152597초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
			cout << arr[i] << '\n';
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 16분 13.8053184초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		cout << n << '\n';
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 16분 20.3269853초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		cout << n << '\n';
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 16분 30.6710386초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		cout << n << '\n';
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 17분 59.8678047초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		print("{} ", n);
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 18분 25.2652922초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		print("{:8}", n);
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 19분 00.4820563초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		print("{:^80}", n);
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 10일 화요일 15시 19분 21.2269366초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		print("{:<80}", n);
	}


	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 11시 33분 23.1131682초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라

	int max = arr[0];

	for (const int& n : arr) {
		if (n > max) {
			max = n;
		}
	}

	print("\nmax: {}", max);

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 11시 35분 28.0841752초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라

	int max = arr[0];

	for (const int& n : arr) {
		if (n > max) {
			max = n;
		}
	}

	print("\nmax: {}", max);

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 11시 43분 11.2813034초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라

	int max = numeric_limits<int>::min();

	sort(begin(arr), end(arr));

	print("\nmax: {}", arr[999]);

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 11시 43분 25.6211144초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}
	sort(begin(arr), end(arr));

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라

	int max = numeric_limits<int>::min();


	print("\nmax: {}", arr[999]);

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 11시 44분 46.1510215초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	sort(begin(arr), end(arr)); // n알고리즘이던게 복잡도가 올라가버렸다 - 여기서 사용하기에는 빵점짜리 코드

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라



	print("\nmax: {}", arr[999]);

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 03분 27.1558395초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 큰 값을 찾아 화면에 출력하라

	auto maxValuePosition = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *maxValuePosition << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 04분 46.5043610초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 05분 05.1372070초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<80}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 작은 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 05분 25.7782777초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"

default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999}; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 작은 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 09분 57.7569703초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 작은 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 02.0317964초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 작은 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 06.4392967초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = min_element( begin(arr), end(arr) );

	cout << "가장 작은 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 27.0456312초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 29.9232021초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 32.6713228초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 35.7497403초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 38.6655908초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 10분 41.0989610초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 작은 값을 찾아 화면에 출력하라

	auto mVP = max_element( begin(arr), end(arr) );

	cout << "가장 큰 값 : " << *mVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 13분 59.4122745초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 14분 04.1925621초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 14분 28.1714412초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 14분 31.5348579초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 14분 35.1283327초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre{ random_device{}() };
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 14분 49.0722808초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 23분 47.5030200초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto mVP = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << mVP.second << '\n';
	cout << "가장 작은 값 : " << mVP.first << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 23분 57.1627338초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>

using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	int arr[1000];

	for (int i = 0; i < 1000; ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라

	const auto mVP = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *mVP.second << '\n';
	cout << "가장 작은 값 : " << *mVP.first << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 32분 04.8832389초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array> 
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	//int arr[1000];	// 이 자료구조는 사용하지 말자
	array <int, 1000> arr;

	for (int i = 0; i < arr.size(); ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라
	// structed binding - syntactic sugar
	const auto [minVP, maxVP] = minmax_element( begin(arr), end(arr) );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 54분 19.5181261초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array> 
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

template <class T, int N>
class myArray {
public:
	T& operator[](int idx){
		return arr[idx];
	}

	T* begin() {
		return &arr[0];
	}

	T* end() {
		return &arr[N];
	}
	
	size_t size() {
		return N;
	}

private:
	T arr[N];
};

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	//int arr[1000];	// 이 자료구조는 사용하지 말자
	myArray <int, 1000> arr;

	for (int i = 0; i < arr.size(); ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라
	// structed binding - syntactic sugar
	const auto [minVP, maxVP] = minmax_element( arr.begin(), arr.end() );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 55분 00.9818220초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array> 
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

template <class T, int N>
class myArray {
public:
	T& operator[](int idx){
		return arr[idx];
	}

	T* begin() {
		return &arr[0];
	}

	T* end() {
		return &arr[N];
	}
	
	size_t size() {
		return N;
	}

private:
	T arr[N];
};

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	//int arr[1000];	// 이 자료구조는 사용하지 말자
	myArray <int, 10000> arr;

	for (int i = 0; i < arr.size(); ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라
	// structed binding - syntactic sugar
	const auto [minVP, maxVP] = minmax_element( arr.begin(), arr.end() );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 55분 10.9745969초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array> 
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

template <class T, int N>
class myArray {
public:
	T& operator[](int idx){
		return arr[idx];
	}

	T* begin() {
		return &arr[0];
	}

	T* end() {
		return &arr[N];
	}
	
	size_t size() {
		return N;
	}

private:
	T arr[N];
};

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	//int arr[1000];	// 이 자료구조는 사용하지 말자
	myArray <int, 10000> arr;

	for (int i = 0; i < arr.size(); ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라
	// structed binding - syntactic sugar
	const auto [minVP, maxVP] = minmax_element( arr.begin(), arr.end() );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 12시 57분 44.0957938초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <algorithm>
#include <array> 
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 9999 }; // 클래스인데도 <int> 뺄 수 있다

template <class T, int N>
class myArray {
public:
	T& operator[](int idx){
		return arr[idx];
	}

	T* begin() {
		return &arr[0];
	}

	T* end() {
		return &arr[N];
	}
	
	size_t size() {
		return N;
	}

private:
	T arr[N];
};

//--------
int main()
//--------
{
	// [문제] 랜덤 값을 갖는 int 1000개를 만들어라. 모든 값을 화면에 출력하라

	//int arr[1000];	// 이 자료구조는 사용하지 말자
	myArray <int, 10000> arr;

	cout << "arr의 크기 - " << sizeof(arr) << '\n';

	for (int i = 0; i < arr.size(); ++i) {
		arr[i] = uid(dre);
		//if (arr[i] < 0 )
		
	}

	for (const int& n : arr){
		print("{:<8}", n);
	}

	// 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하라
	// structed binding - syntactic sugar
	const auto [minVP, maxVP] = minmax_element( arr.begin(), arr.end() );

	cout << endl;
	cout << "가장 큰 값 : " << *maxVP << '\n';
	cout << "가장 작은 값 : " << *minVP << '\n';

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 03분 18.0095860초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 04분 19.6095030초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 10'000'000; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 07분 36.0546676초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 1000; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 08분 47.2525689초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 1000; ++i) {
		out << uid(dre) << endl;
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 09분 52.2239670초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 1000; ++i) { // 엔터키와 스페이스의 공간 차이가 다르더라
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 10분 46.1965650초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] "int값1000만개.txt"에 저장한 int값을 읽어 화면에 저장하라

	std::ofstream out{ "int값1000만개.txt", std::ios::trunc };
	if (not out) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	for (int i = 0; i < 1000'0000; ++i) { // 엔터키와 스페이스의 공간 차이가 다르더라
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}


======================================================================
저장시간 - 2026년 03월 11일 수요일 13시 15분 57.8390197초
======================================================================

//------------------------------------------------------
// 2026 1학기 화56수34                 3월 4일          (2주 1일)
//------------------------------------------------------
// 많은 자료를 처리하기
// -----------------------------------------------------
#include <iostream>
#include <random>
using namespace std;

#include "save.h"


default_random_engine dre;
uniform_int_distribution<int> uid{ 0, 999'9999 }; // 클래스인데도 <int> 뺄 수 있다



//--------
int main()
//--------
{
	// [문제] "int값1000만개.txt"에 저장한 int값을 읽어 화면에 저장하라

	std::ifstream in{ "int값1000만개.txt" };

	if (not in) {
		std::cout << "\" - 열 수 없습니다." << '\n';
		exit(20260304);
	}

	int n;
	while (in) {
		in >> n;
	}
		cout << n << ' ';

	save("메인.cpp");
}


